

name: CIimport React from "react"; import { motion } from "framer-motion";

export default function LandingPage() { return ( <div className="min-h-screen flex flex-col bg-gray-50 text-gray-900"> {/* Hero Section */} <header className="flex flex-col items-center justify-center text-center py-20 px-6 bg-gradient-to-r from-indigo-500 to-purple-600 text-white"> <h1 className="text-4xl md:text-6xl font-bold mb-4"> Управляй Telegram-ботами в один клик </h1> <p className="text-lg md:text-2xl max-w-2xl mb-6"> Простая SaaS-панель для автоматизации и контроля </p> <motion.a whileHover={{ scale: 1.05 }} whileTap={{ scale: 0.95 }} href="#pricing" className="bg-white text-indigo-600 font-semibold px-6 py-3 rounded-2xl shadow-lg" > Попробовать бесплатно </motion.a> </header>

{/* Features Section */}
  <section className="py-16 px-6 max-w-6xl mx-auto">
    <h2 className="text-3xl font-bold text-center mb-10">Возможности панели</h2>
    <div className="grid md:grid-cols-4 gap-8">
      {[
        { title: "Управление ботами", desc: "Лёгкий контроль всех ботов в одном месте" },
        { title: "Аналитика", desc: "Подробная статистика и отчёты" },
        { title: "Автоматизация", desc: "Сценарии и автозадачи без кода" },
        { title: "API", desc: "Интеграция с вашими сервисами" },
      ].map((f, i) => (
        <motion.div
          key={i}
          whileHover={{ y: -5 }}
          className="bg-white rounded-




